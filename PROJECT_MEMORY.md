# 项目持久记忆 Project Memory

> 本文件由 dsh-memoir 插件维护：记录本项目历次会话的工作归纳、经验教训与行动指南，
> 作为未来 AGENTS 接手本项目时的行动指南。会话开始时自动注入 system prompt。

## 工作记录 Work Log

- [2026-08-28 18:06] [工作记录] 安装 music-processing-skills 到 DSH + 调研 gdstudio 两个仓库 — 1) 已把 ~/.config/opencode/skills/music-processing-skills 完整复制到 ~/.dsh/skills/music-processing-skills/（含 SKILL.md、两个下载器、metaflac 嵌入脚本、测试、retry_failed.sh、.gd-flac-cache 缓存脚本），DSH 会话技能目录已出现 music-processing-skills。工作区仓库（TommyDucx/automatic-downloading-music）与 opencode 源文件逐字节一致，无需再同步。2) 调研结论见工作区 GDSTUDIO_REFERENCE.md：gdstudio-lx-source 仅确认 api.php 形态与 br 档位（740=16bit FLAC、999=24bit FLAC、限流 50次/5分钟）；gdstudio-embeded-service 的 client.go 展示 search/url/pic/lyric 四类 API、封面 size 回退+Referer、tlyric 翻译、镜像分流 cn/hk/us、metaflac 标签流程（与我们的实现同思路）；其签名（硬编码 version 20251104 + 标准 MD5）易腐化，不建议照搬，保持自动拉 crc32.min.js/player.js 的方案。下一步可补：封面内嵌、LYRICS_TRANSLATED、镜像分流。
- [2026-08-28 18:15] [工作记录] 落地封面/翻译/镜像三大功能到 music-processing-skills — 依据 gdstudio-embeded-service 参考价值，已把功能落入技能并同步 ~/.dsh/skills/music-processing-skills/：1) flac_metadata_embedder.py 新增 GDMusicClient（types=search/pic/lyric，crc32 签名 s=crc32Hex(encodeURIComponent(name|id))，镜像分流 cn/hk/us，401/网络错误指数退避 1s→30s），支持封面内嵌（PICTURE 块：--remove --block-type=PICTURE 后 --import-picture-from，尺寸 1000/640/500/300 回退，按源带 Referer）、LYRICS_TRANSLATED（tlyric）、歌词兜底；pick_metadata 先曲名等值+歌手部分匹配再兜底带 id 条目；新增 --gd-source/--no-cover/--no-gdmusic 参数；顺带修复 sys 未导入的 bug。2) gd-international-downloader.js 新增镜像分流（migu/kugou/ximalaya→cn，joox→hk，qobuz/ytmusic→us，第 5 参数可手动指定）。3) SKILL.md/README.md/GDSTUDIO_REFERENCE.md 同步更新。
- [2026-08-28 19:10] [工作记录] 完成上一批功能的验证并修复 3 个 bug，已 git 提交 — 用 gd-flac-downloader.js 实测下载「周杰伦 - 晴天」joox FLAC 999kbps 53MB，跑 --single-file 内嵌：1) 修复 metaflac 中文标签损坏（根因：metaflac 在非 UTF-8 locale（C）下把非 ASCII 字节替换为 #/?，DSH 沙箱默认 LC_CTYPE=C 且逐调用不稳定）→ 所有 metaflac 子进程强制 env LC_ALL=en_US.UTF-8（METAFLAC_ENV），验证中文标签原始字节正确（晴天/周杰伦/魔杰座）；2) 修复 _clean_lyrics_for_tag 把 LRC 整行（[时间戳]+文本）丢弃导致 LYRICS 永不写入 → 剥离 [..] 前缀保留文本，验证 LYRICS=故事的小黃花… 已内嵌；3) embed_metadata 重排：封面(PICTURE 重写)在前、标签最后写，避免干扰。验证结果：py_compile ✅；PICTURE 封面块 ✅（71317B）；LYRICS ✅；LYRICS_TRANSLATED 缺失 = GD API 该歌 tlyric 为空（数据问题，代码读取正确，需选有翻译的歌验证）。已将修复同步到 ~/.dsh/skills/music-processing-skills/，并 git 提交本批改动。

## 经验教训 Lessons Learned

- [2026-08-28 18:15] [经验教训] 踩坑：模板字面量内嵌反引号会解析失败 — 在 run_code 里用 String.raw 模板字面量写文件时，内容里的反引号（如 Python 的 value.replace("`","'")、Markdown 代码片段）会提前终止模板导致 'Expression expected'。对策：内容用多段 String.raw 分块并逐块探测，反引号改用 \x60 转义，Markdown 用行数组 join 构造。另外 write/edit 等文件工具必须包在 run_code 里调用（本会话文件权限为 danger-full-access）。
- [2026-08-28 19:10] [经验教训] metaflac 中文标签损坏的根因是 locale 而非 metaflac 命令本身 — metaflac（Homebrew flac 1.5.0）在非 UTF-8 locale（LC_CTYPE=C）下写入标签时，会把非 ASCII 字节逐个替换为 '#'（导出/显示时表现为 '?'）；DSH 沙箱 bash 的 locale 环境逐调用不稳定（默认 C），导致同一命令时好时坏、极难排查。验证方法：绕过 metaflac 的 --export-tags-to/--list（它们本身也受 locale 影响做转换），直接用 python 读文件里 TITLE= 后的原始字节。修复：metaflac 子进程强制 env={'LC_ALL':'en_US.UTF-8','LANG':'en_US.UTF-8'}。教训：涉及非 ASCII 的子进程写入必须显式指定 UTF-8 locale，且校验要读原始字节而非依赖工具的显示输出。另：DSH 沙箱内 py_compile 需 PYTHONPYCACHEPREFIX=/tmp（macOS 系统 Python 把 pycache 写到 ~/Library/Caches/com.apple.python 被沙箱拦截）；posix_spawn failed 多来自 node-pty 终端（spawn-helper 可执行位被 pnpm 剥掉或沙箱内 posix_openpt 被拒），与 agent 的 bash 工具无关。

## 行动指南 Action Guide

- [2026-08-28 18:15] [行动指南] 下一步：验证封面/翻译新功能并提交 — 本次会话 bash 工具不可用（posix_spawn failed），无法跑 py_compile 与真实 API 联测。验证步骤：1) python3 -m py_compile flac_metadata_embedder.py；2) 用 --single-file 对一首已下载歌曲试跑，确认封面 PICTURE 与 LYRICS_TRANSLATED 出现；3) metaflac --list --block-type=VORBIS_COMMENT 检查；4) 若 music-api.gdstudio.xyz 的 types=pic/lyric 不接受 crc32 签名，改用 gdstudio-embeded-service 的 MD5 签名方案（version 20251104 + hostname|version|ts9|id 顺序）并回写脚本；5) git 提交本批改动（工作区是 TommyDucx/automatic-downloading-music 仓库）。
- [2026-08-28 19:10] [行动指南] 验证已完成并提交，后续注意：1) 沙箱内跑 python 一律 PYTHONPYCACHEPREFIX=/tmp；2) 校验 metaflac 标签用 python 读原始字节（metaflac 导出受 locale 影响）；3) LYRICS_TRANSLATED 需选 GD API 有 tlyric 的歌（如非中文歌）验证；4) 若再遇 posix_spawn failed 终端问题：检查 profile node_modules/node-pty/prebuilds/<平台>/spawn-helper 可执行位，缺失则 chmod +x 或跑 dsh-better-sidebar 的 scripts/install.sh --repair；5) DSH Desktop 2.0.3 应用内置 node-pty 1.2.0-beta.15 存在 helper 路径问题（app.asar.unpacked 直载时二次 replace），应用内终端可能受此影响，建议关注 DSH 官方 issue #2851 对应修复。
