# 音源调研（GitHub）2026-09-19

调研目的：为「批量下载 FLAC + metaflac 内嵌元数据」这条流水线找**能真正拿到无损/Hi-Res 的高质量音源**，
并判断哪些值得接进来。调研手段：GitHub API 搜索 + 读源码/README + **真实网络实测**。

---

## 一、结论速览

| 排名 | 音源 | 类型 | 最高音质 | 要账号/密钥吗 | 有 WAF 吗 | 接入状态 |
|---|---|---|---|---|---|---|
| 🥇 | **GD音乐台**（music.gdstudio.xyz） | 聚合站 API | netease/qobuz/tidal 24bit 真无损 | **什么都不需要** | ✅ Cloudflare（需浏览器内核） | ✅ **已实现** `gd-browser-downloader.js` |
| 🥈 | **ChKSz API**（api.chksz.com） | 普通 HTTPS 接口 | 网易云 `jymaster` 母带 / QQ·酷狗 `master` | 免**费** apikey，但**邮箱注册已关闭**，只能 LinuxDo 登录 | ❌ 无 | ⚠️ 代码已实现 `chksz-downloader.js`，等 key |
| 🥉 | 酷狗概念版（MakcRe/KuGouMusicApi） | 官方 API 逆向 | flac / hires | 不需要账号 | ❌ | ❌ **客户端签名已过期**，搜索接口 152，跑不通 |
| 4 | streamrip / qobuz-dl | 官方 API 客户端 | Qobuz/Tidal Hi-Res（最高） | 需**付费订阅** | ❌ | 暂不接（成本） |
| 5 | cyapi.top（QQ 解析，共享 key） | 第三方解析 | **只有 320k 有损** | 免 key（key 公开在脚本里） | ❌ | 可作补漏通道 |
| 6 | 付费 lx 音源（聆澜 / IKUN） | 第三方聚合 | 视卖家 | 卡密（1.5~15 元） | ❌ | 不推荐长期依赖 |

**一句话**：GD音乐台是当前唯一「零账号 + 免费 + 真 24bit 无损」的路子，且已经跑通；
ChKSz 音质更高但入口被官方关了（只能 LinuxDo 登录）；酷狗那条路客户端签名过期，暂时走不通。


---

## 二、逐个说明

### 1. ChKSz API —— 代码已就绪，但注册入口被官方关了（见第五节）
- 站点：https://api.chksz.com ｜ 文档：https://api.chksz.com/docs
- 生态项目：[ChKSz/CPlayer](https://github.com/ChKSz/CPlayer)（67★，在线播放器）、
  [ChKSz/CDown](https://github.com/ChKSz/CDown)（15★，PyQt 网易云下载器）、
  [flashlab/music-api](https://github.com/flashlab/music-api)（洛雪协议封装，单文件无后端）
- 接口（全部 `GET/POST`，无 WAF，普通 curl 就能通，参数 `apikey` 必填）：
  | 端点 | 作用 |
  |---|---|
  | `/api/163_search` | 网易云搜索（keyword / limit / offset） |
  | `/api/163_music` | 取音频地址 + 详情（id / level / type=json\|text\|跳转） |
  | `/api/163_lyric` | 歌词（原文 / 翻译 / 罗马音） |
  | `/api/163_playlist` | 歌单详情 |
  | `/api/qq_music` | QQ 音乐点歌 |
  | `/api/kugou_music` | 酷狗点歌 |
- 音质档位：网易云 `standard / exhigh / lossless / hires / jyeffect / sky / jymaster`；
  QQ：`128k / 320k / flac / hires / master`；酷狗：`128k / 320k / flac / hires / master`
- 有「每日免费额度」，密钥登录后自助查看
- 实测：无 key → `401 {"code":401,"msg":"缺少 apikey 参数，请先登录并查看个人密钥"}`；旧域名 api.chksz.top 已废弃（nginx 404）
- 落地：`chksz-downloader.js`（本仓库）。取 key 后即可用：
  ```bash
  export CHKSZ_KEY=你的密钥      # https://api.chksz.com 登录后查看
  node chksz-downloader.js --list songs.json --out "downloads/01-xxx" --level jymaster --lyrics
  ```
  已用本地 mock 全链路验证：搜索→匹配打分（正确排除翻唱/Live）→音质逐档降级→下载→魔数校验→写 `.downloaded.json`→写 `.lrc`。

### 2. GD音乐台 —— 已在用，但只能走浏览器
详见 `SKILL.md`「网络拓扑 / 签名算法」两节。要点：三个域名（org / xyz / music-api）的 `/time` 与 `/api.php`
全被 Cloudflare 拦，脚本直连一律 403；签名是站点改造过的 HMAC-MD5，无法离线复刻 → 必须借页面里的 `crc32()`。
优点：**零账号**就能拿到 netease / joox / qobuz / tidal / apple / ytmusic / kuwo / tencent 八家。
实测 jOOX 能给到 999kbps 24bit FLAC，Qobuz 给到 37MB 真 FLAC。

### 3. 酷狗概念版（MakcRe/KuGouMusicApi，966★）
- 官方 API 逆向，`git clone && npm install`，`.env` 里 `platform=lite` 切成概念版，`npm run dev` 起在 :3000
- **EchoMusic 用的就是这套**（内嵌为子模块，见 `ECHOMUSIC_REFERENCE.md`）
- 不需要账号，但风控靠**设备指纹持久化**（guid/mid/webglHash）+ 行为指纹（`generate_simulate.js`）
- 接入成本：要维护一个本地常驻服务 + 指纹逻辑，比 ChKSz 重；收益是与 GD音乐台互补的独立曲库
- 结论：**列为候选**，等 ChKSz 用顺手后再说

### 4. 官方 API 系（自己实现签名/带 cookie）
- [Binaryify/NeteaseCloudMusicApi](https://github.com/Binaryify/NeteaseCloudMusicApi) 30259★（已停止维护）
- [NeteaseCloudMusicApiEnhanced/api-enhanced](https://github.com/NeteaseCloudMusicApiEnhanced/api-enhanced) 1815★（活跃 fork）
- [Rain120/qq-music-api](https://github.com/Rain120/qq-music-api) 1081★
- 局限：**无损/Hi-Res 要登录 cookie（多数要 SVIP）**，白嫖不到；不如直接用 ChKSz 这种已「代持会员」的聚合接口

### 5. Hi-Res 专业下载器（要付费订阅）
- [nathom/streamrip](https://github.com/nathom/streamrip) 4934★（Qobuz/Tidal/SoundCloud/Deezer）
- [vitiko98/qobuz-dl](https://github.com/vitiko98/qobuz-dl) 2366★
- [BartolomeoRusso9/SpotiFLAC-Module-Version](https://github.com/BartolomeoRusso9/SpotiFLAC-Module-Version) 212★（Spotify 元数据 + Tidal/Qobuz/Deezer/Amazon 供货）
- 优点：母带级、元数据最规范；缺点：要买 Qobuz/Tidal 订阅。**GD音乐台的 qobuz/tidal 通道等价于「别人代持账号」，目前免费**。

### 6. LX Music 音源生态（了解即可，建议不依赖）
- [Macrohard0001/lx-ikun-music-sources](https://github.com/Macrohard0001/lx-ikun-music-sources) 2448★，更新极勤，但主体是**付费音源导购**（聆澜 1.5~15 元、IKUN 5~15 元卡密）
- [cdyUuu/lx-music-xinghai-source](https://github.com/cdyUuu/lx-music-xinghai-source) 146★，聚合 GDAPI + ChKSz，**亮点是支持酷我加密音频 `.mflac`/`.mgg` 解密**（需自建 [kuwo-music-relay](https://github.com/cdyUuu/kuwo-music-relay) PHP 中转，`KW_DECRYPT_PROXY` + `allowEncryptedLossless`）
- 这类脚本的 API 都是「GDAPI / ChKSz / 私有聚合」，本质在复用上面第 1、2 条；直接对接它们的上游更稳定
- 第三方免费解析站（不稳定，仅备选）：`music.tmetu.cn`、`wyapi.toubiec.cn`、`mscdownload.pages.dev`、`jx.chksz.top`

### 7. 相关项目（非音源）
- [gdstudio-org/Embeat](https://github.com/gdstudio-org/Embeat) 302★ —— GD Studio 官方的**音乐推荐模型**（Spotify 45M 曲目训练，输入曲目出相似曲）。可用来「按风格扩展歌单」，不是音源
- [hoowhoami/EchoMusic](https://github.com/hoowhoami/EchoMusic) —— 桌面播放器，音源走酷狗概念版；本次已升级到 v2.3.1

---

## 三、落地建议

1. **日常批量下载首选 `gd-browser-downloader.js`**（GD音乐台）：零账号、免费、能到 qobuz/tidal 的
   24bit/48kHz 真 Hi-Res，且默认「全源扫描 → 按实际码率择优」。
2. **ChKSz 想要就用 `chksz-downloader.js`**，但先得有 key —— 现在**邮箱注册已被官方关闭**，
   唯一入口是注册 linux.do 论坛账号后用 OAuth 登录，然后在账户页「查看密钥」。
   拿到后 `export CHKSZ_KEY=...` 即可，代码与 mock 测试都已就绪。
3. 两个脚本 CLI 与输出约定完全一致（`歌手 - 歌名.扩展名` + `.downloaded.json`），可混用后再统一跑
   `flac_metadata_embedder.py` 内嵌元数据。
4. **不建议**为了拿 key 去逆向/绕过 ChKSz：服务端签发 + 服务端校验，客户端没有可提取的凭据，
   绕过也是白嫖他人赞助的额度。
5. 有 Qobuz/Tidal 订阅的话，`streamrip` 是画质天花板，但它不解决「找歌 + 中文元数据」，
   与本流水线是互补而非替代。


---

## 四、实测记录（可供复查）

```
# ChKSz 无 key
GET https://api.chksz.com/api/163_search?keyword=test
→ 401 {"code":401,"msg":"缺少 apikey 参数，请先登录并查看个人密钥"}

# GD音乐台（Node 直连，真实网络）
GET https://music.gdstudio.xyz/time        → 403 (Cloudflare "Just a moment...")
GET https://music.gdstudio.xyz/js/player.js → 200   ← 静态资源不过 WAF，容易误判
（同一时刻用浏览器内核 CDP 在页面里 fetch('/api.php') → 200 + 正确 JSON）

# GD音乐台取流（国际版 site=xyz）
netease "Resonance - HOME" 999 → FLAC 636kbps 16MB，CDN 直连 200 魔数 fLaC
qobuz   "Resonance - HOME" 999 → FLAC 999kbps 37MB（akamaized）
joox    "晴天 - 周杰伦"     999 → FLAC 999kbps 53MB 24bit/44.1kHz
```

---

## 五、ChKSz 能不能"逆向拿到 key"？—— 结论：不能（2026-09-19 实测）

用户注册不了账号，要求直接逆向。做了以下排查，结论是否定的：

1. **key 是服务端签发的**，前端（api.chksz.com / CPlayer / 官方 lx 脚本 `flashlab/music-api`）
   全部是「用户把 key 填进来」的模式，没有任何内置/可逆的客户端凭据可提取。
2. **邮箱注册已被官方暂停**。在真实 Chrome 里打开 https://api.chksz.com/login ，
   读取页面全局变量得到：
   ```
   emailRegistrationEnabled = false
   turnstileSiteKey = 0x4AAAAAAD4_zP79l-B1uACc
   调用 showRegisterForm() → 提示「当前已暂停邮箱注册」，注册表单不激活
   ```
   即**只剩 LinuxDo OAuth 一条路**（登录页的「使用 LinuxDo 继续」）。
   所以"注册不了"不是操作问题，是站点策略。
3. 绕过 apikey 等于白嫖别人赞助的会员额度，且服务端校验，客户端无从下手 —— 不做了。

> 想用 ChKSz 的唯一正路：注册一个 linux.do 论坛账号，用它的 OAuth 登录后再「查看密钥」。

## 六、免费"在线解析站"逐个实测（全部为逆向其前端/后端所得）

| 站点 | 逆向结果 | 能不能用 |
|---|---|---|
| `music.tmetu.cn` | 有自建后端 `/api/?miss=search`（**免 key**，实测搜索通） | ❌ 取流接口 `/api/?miss=songAll` 恒返回网易云 `code:-462 检测到您的网络环境存在风险`（服务端 IP 被网易云标记），取不到地址 |
| `wyapi.toubiec.cn` | React 前端，调 `/api/getSongUrl` | ❌ 该路径 404（后端已换/下线） |
| `api.injahow.cn/meting`（Meting） | 老牌聚合 | ❌ 404 |
| `kw-api.cenguigui.cn` | 酷我解析 | ❌ 404 |
| `api.cenguigui.cn/api/netease/music_v1.php` | 网易云解析 | ⚠️ 接口活着，但 `url` 字段返回「获取歌曲地址失败，可能是会员到期了」 |
| `cyapi.top/API/qq_music.php` | QQ 音乐解析，**apikey 是公开写在 lx 脚本里的共享 key** | ✅ **可用**，但**只有 320k 有损**（URL 前缀 `C400`，非 FLAC），只能当补漏 |
| `api.music.lerd.dpdns.org` | 「聚合API接口(CF)」 | ⚠️ 只有 Cloudflare 200 探活页，未找到接口路径 |
| `mscdownload.pages.dev` | 纯前端 | ❌ 未发现可用后端 |

## 七、酷狗概念版（KuGouMusicApi）实测 —— 当前不可用（客户端签名过期）

按 README 完整跑了一遍：克隆 → `npm install`（311 包）→ `.env` 里 `platform='lite'` → `node app.js`。

| 接口 | 结果 |
|---|---|
| `/register/dev` | ✅ 200，返回真实 `dfid` |
| `/server/now` | ✅ 200 |
| `/search/suggest` | ✅ 200（能出补全） |
| `/search/hot`、`/search/default` | ✅ 200 |
| **`/search`（取歌曲 hash 的关键接口）** | ❌ `{"status":1,"error_code":152,"error_msg":"Parameter Error","data":{"lists":[]}}` |
| `/search/complex` | ❌ 同样 152，`lists` 全空 |

标准版与概念版（lite）都试了、补了 MAC/WebGL 设备指纹也无效 → **酷狗侧已升级校验，仓库里的 clientver(20489)/签名盐已过期**。
要修好等于重新逆向酷狗当前客户端（含行为指纹），这正是 EchoMusic 团队在持续维护的工作量，不适合顺手做。

